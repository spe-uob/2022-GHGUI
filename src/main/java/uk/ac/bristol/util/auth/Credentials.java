package uk.ac.bristol.util.auth;

/** Shows that a class can be cast to a byte stream. */
public interface Credentials {
  /** A function to include import the credentials back into the project. */
  void reimport();
}
